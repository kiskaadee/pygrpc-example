[project]
name = "pygrpc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kiskaadee", email = "fcortesbio@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "grpcio>=1.83.1",
    "protobuf>=7.36.1",
]

[project.scripts]
pygrpc = "pygrpc:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
